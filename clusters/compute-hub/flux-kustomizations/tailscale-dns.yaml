apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tailscale-dns
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/compute-hub/tailscale-dns
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: tailscale-operator
