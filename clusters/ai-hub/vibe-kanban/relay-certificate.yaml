apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vibe-relay-tls
  namespace: vibe-kanban
spec:
  secretName: vibe-relay-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - relay.nwlnexus.net
