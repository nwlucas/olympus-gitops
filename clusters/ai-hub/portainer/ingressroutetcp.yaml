apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: portainer-edge-tunnel
  namespace: portainer
spec:
  entryPoints:
    - portainer-edge
  routes:
    - match: HostSNI(`*`)
      services:
        - name: portainer
          port: 8000
