apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vibe-relay
  namespace: vibe-kanban
  annotations:
    kubernetes.io/ingress.class: traefik
    external-dns.alpha.kubernetes.io/hostname: vk-relay.nwlnexus.net
    external-dns.alpha.kubernetes.io/target: ing.mgmt.nwlnexus.net
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`vk-relay.nwlnexus.net`)
      kind: Rule
      services:
        - name: vibe-kanban-relay
          port: 8082
  tls:
    secretName: vibe-relay-tls
