apiVersion: v1
kind: Service
metadata:
  name: plex-styx
  namespace: media
spec:
  ports:
    - name: plex
      port: 32400
      targetPort: 32400
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: plex-styx
  namespace: media
subsets:
  - addresses:
      - ip: 100.72.84.56   # styx Tailscale IP — direct LAN peer connection, no DERP relay
    ports:
      - name: plex
        port: 32400
        protocol: TCP
