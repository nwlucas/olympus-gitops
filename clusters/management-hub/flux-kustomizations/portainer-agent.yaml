apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: portainer-agent
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/management-hub/portainer-agent
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: tailscale-operator
