apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: connect-ingress
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/management-hub/connect-ingress
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: cert-manager
