deployment:
  replicas: 1

service:
  type: ClusterIP

ports:
  web:
    port: 80
    expose:
      default: true
  websecure:
    port: 443
    expose:
      default: true

api:
  dashboard: true
  insecure: true

logs:
  general:
    level: INFO
